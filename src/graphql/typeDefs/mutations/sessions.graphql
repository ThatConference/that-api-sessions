type SessionsMutation {
  create(eventId: ID!, session: SessionCreateInput!): Session!
    @auth(requires: "sessions")
  delete(id: ID!): ID @auth(requires: "sessions")
  session(id: ID!): SessionMutation @auth(requires: "sessions")
}
