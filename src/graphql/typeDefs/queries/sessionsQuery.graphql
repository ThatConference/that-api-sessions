type SessionsQuery {
  "will return the sessions associated to the logged in user"
  me: MeQuery @auth(requires: "sessions")
  "will return an ACCEPTED, SCHEDULED, or CANCELLED session for matching slug"
  session(slug: String!): AcceptedSession
}
